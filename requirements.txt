# TikTok 选品自动化 - 依赖清单
# Python 3.11+

# HTTP 客户端
httpx>=0.27,<1.0
httpx-sse>=0.4

# 数据解析
pydantic>=2.0,<3.0
pyyaml>=6.0
beautifulsoup4>=4.12

# 定时调度
apscheduler>=3.10

# OpenAI / ChatGPT API
openai>=1.0

# 数据库
sqlalchemy>=2.0
aiosqlite>=0.20

# 工具
tenacity>=8.0  # 指数退避重试
python-dotenv>=1.0

# 通知（微信企业号机器人）
httpx>=0.27  # 已包含

# 可选：Playwright (用于 EchoTik 无头浏览器抓取)
# playwright>=1.40

# 可视化看板
streamlit>=1.30,<2.0
streamlit-autorefresh>=1.0
plotly>=5.18,<6.0
pandas>=2.0
