# TikTok 选品自动化 — 完整复现手册

版本：v1.0（最后更新：2026-07-25）
GitHub：https://github.com/isabelle017/TK-TH

---

## 一、项目定位

TikTok 选品自动化工具，数据源自动降级，非必须任何付费 API。
**月 GMV < $5k 时零成本运行，超过后再接入付费数据源。**

---

## 二、换电脑后恢复步骤

### 2.1 克隆代码

```bash
git clone git@github.com:isabelle017/TK-TH.git
cd TK-TH
```

> 如果 SSH 连不上 GitHub，先配代理：
> ```bash
> git config --global http.proxy http://127.0.0.1:7890
> git config --global https.proxy http://127.0.0.1:7890
> ```

### 2.2 安装 Python（3.11+）

从 https://www.python.org 下载安装，安装时勾选 "Add to PATH"。

### 2.3 安装依赖

```bash
pip install -r requirements.txt
```

### 2.4 配置环境变量

```bash
cp .env.example .env
```

编辑 `.env`，填入真实值（见下方第五节）。

### 2.5 测试运行

```bash
python scheduler.py --region sea --once
```

---

## 三、日常使用

### 本地运行

```bash
# 一次性运行（东南亚三国）
python scheduler.py --region sea --once

# 一次性运行（指定市场）
python scheduler.py --region sea --once --markets th,vn,my

# 发送测试通知
python scheduler.py --test-notify
```

### 启动看板

```bash
streamlit run dashboard.py
```

浏览器打开 http://localhost:8501

### GitHub Actions 自动运行（推荐）

推送到 GitHub 后，每天自动在以下时间运行：
- 泰国时间 08:00 / 12:00 / 20:00（SEA 工作流）
- 北京时间 08:00 / 16:00（US 工作流）

手动触发：https://github.com/isabelle017/TK-TH/actions → SEA 选品分析 → Run workflow

---

## 四、评分算法（核心）

### 4.1 评分公式

```
综合评分 = (增长分 × 0.40 + 竞争分 × 0.20 + 利润分 × 0.15 + 互动分 × 0.15) × 季节性系数
```

### 4.2 各维度详解

#### 增长分（权重 0.40 — SEA 最重要维度）

| 7日增长率 | 得分 |
|----------|------|
| ≥ 100% | 100 |
| 50% | 75 |
| 20% | 60 |
| 0% | 40 |
| 负增长 | 线性下降至0 |

#### 竞争分（权重 0.20）

| 在售卖家数 | 得分 | 含义 |
|-----------|------|------|
| 0-5 | 100 | 蓝海 |
| 5-20 | 80 | 低竞争 |
| 20-50 | 60 | 中等 |
| 50-100 | 40 | 中高竞争 |
| 100-200 | 20 | 高竞争 |
| >200 | 5-10 | 红海 |

#### 利润分（权重 0.15 — SEA 权重偏低）

| 售价区间 | 基础分 |
|---------|--------|
| ≥ $50 | 85 |
| $30-$50 | 75 |
| $15-$30 | 65 |
| $8-$15 | 50 |
| $3-$8 | 35 |
| < $3 | 20 |

定价位置调整：高定价 +15，低定价 -15。

#### 互动分（权重 0.15）

| 互动率 | 得分 |
|--------|------|
| ≥ 20% | 100 |
| 10% | 80 |
| 5% | 60 |
| 2% | 40 |
| 1% | 25 |

TikTok 平均互动率约 3-5%。

#### 季节性系数（0.5-2.0）

根据当前月份调整：
- 11月（黑五）：1.8 倍
- 12月（圣诞）：1.5 倍
- 7月（淡季）：0.7 倍
- 其他月份：1.0

### 4.3 评分等级

| 分数 | 标签 | 行动 |
|------|------|------|
| ≥ 82 | 🔥 爆品潜力 | 立即深入调研，上架测试 |
| 72-82 | 📈 趋势上升 | 加入观察清单 |
| 65-72 | 👀 值得关注 | 通知推送 |
| < 65 | — | 不推送，存入数据库 |

### 4.4 SEA vs US 权重对比

| 维度 | SEA | US | 原因 |
|------|-----|----|------|
| 增长 | 0.40 | 0.35 | SEA 市场仍在高速扩张 |
| 竞争 | 0.20 | 0.20 | 持平 |
| 利润 | 0.15 | 0.25 | SEA 客单价低（$5-20） |
| 互动 | 0.15 | 0.10 | SEA 社交电商属性更强 |
| 季节性 | 0.10 | 0.10 | 持平 |

---

## 五、API Key 配置清单

### 5.1 必须配置

| Key | 获取方式 |
|-----|---------|
| **OPENAI_API_KEY** | https://platform.openai.com/api-keys（需代理） |

### 5.2 通知方式（二选一即可）

**方案 A：QQ 邮箱（推荐，国内直连）**

| 变量 | 值 |
|------|-----|
| `SMTP_HOST` | `smtp.qq.com` |
| `SMTP_PORT` | `465` |
| `SMTP_USER` | `你的QQ号@qq.com` |
| `SMTP_PASSWORD` | QQ邮箱 → 设置 → 账户 → 生成授权码 |

**方案 B：Telegram（需代理）**

1. Telegram 搜索 @BotFather，发 `/newbot` 创建
2. 拿 Token：`123456789:ABCdef...`
3. 给 Bot 发一条消息
4. 打开 `https://api.telegram.org/bot<TOKEN>/getUpdates`
5. 取 `chat.id` 数字

| 变量 | 值 |
|------|-----|
| `TELEGRAM_BOT_TOKEN` | Bot Token |
| `TELEGRAM_CHAT_ID` | Chat ID 数字 |

### 5.3 数据源（全选配，不配走 Mock）

| 数据源 | 配法 | 费用 |
|--------|------|------|
| EchoTik | 浏览器登录后复制 Cookie | 免费 |
| Kalodata API | 官网申请 | $49-$199/月 |
| Tabcut API | 官网申请 | $0-$99/月 |

---

## 六、项目文件速查

| 功能 | 命令 |
|------|------|
| 运行看板 | `streamlit run dashboard.py` |
| 运行 SEA 一次 | `python scheduler.py --region sea --once` |
| 运行 US 一次 | `python scheduler.py --region us --once` |
| 测试邮件通知 | `python scheduler.py --test-notify` |
| 泰国策略报告 | `python -c "from th_market_optimizer import *; print(get_thailand_optimizer().generate_report(500))"` |
| 越南 COD 测试 | `python -c "from vn_cod_risk_manager import *; m=get_cod_manager(); r=m.assess_risk('test',300000,'Ha Noi','fashion',True); print(r.risk_level.value, r.risk_score)"` |
| 查看数据库 | `sqlite3 data/tk_products_sea.db "SELECT * FROM products ORDER BY score DESC LIMIT 10;"` |

### 关键文件位置

| 文件 | 用途 |
|------|------|
| `config.sea.yaml` | 东南亚配置（评分权重/市场/通知） |
| `config.us.yaml` | 欧美配置 |
| `scheduler.py` | 主入口 & 调度器 |
| `dashboard.py` | Streamlit 看板 |
| `product_research/analyzer_trend.py` | 评分算法 |
| `product_research/fetcher_fastmoss.py` | FastMoss 占位符 |
| `product_research/fetcher_echotik.py` | EchoTik 抓取 |
| `th_market_optimizer.py` | 泰国深度优化 |
| `vn_cod_risk_manager.py` | 越南 COD 风控 |
| `.env` | 本地密钥（已 gitignore） |
| `data/tk_products_sea.db` | SEA 数据库文件 |

---

## 七、GitHub Secrets 配置

在 https://github.com/isabelle017/TK-TH/settings/secrets/actions 添加：

| Secret | 说明 |
|--------|------|
| `OPENAI_API_KEY` | OpenAI / DeepSeek API Key |
| `SMTP_HOST` | `smtp.qq.com` |
| `SMTP_PORT` | `465` |
| `SMTP_USER` | QQ邮箱地址 |
| `SMTP_PASSWORD` | QQ邮箱授权码 |
| `TELEGRAM_BOT_TOKEN` | （可选）Telegram Bot Token |
| `TELEGRAM_CHAT_ID` | （可选）Telegram Chat ID |

---

## 八、数据来源说明

```
数据源选择逻辑（pipeline 自动处理）：

1. FastMoss（enabled: false）→ 已禁用，无公开 API
2. EchoTik（enabled: true）  → 需配置 Cookie，否则跳过
3. Mock 数据                  → 终极兜底，保证系统永远可运行
```

**Mock 数据的限制：**
- 商品标题和价格是随机生成的
- 不代表真实市场情况
- 仅用于测试管道流程和看板展示
- GMV > $5k 后建议接入真实数据源

---

## 九、后续扩展（GMV > $5k 后再做）

1. **EchoTik Cookie** — 获取真实趋势数据
2. **Kalodata / Tabcut API** — 付费数据源
3. **Telegram 通知** — 实时推送
4. **印尼市场** — 斋戒月策略 + GoPay/OVO/Dana 本地支付
5. **菲律宾/新加坡市场** — 更多 SEA 国家
6. **Windows 定时任务** — 替代 GitHub Actions（本地长期运行）
